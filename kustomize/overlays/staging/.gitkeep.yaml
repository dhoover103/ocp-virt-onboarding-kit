# This file ensures the staging directory is committed to Git
# Remove this file when adding actual staging overlay configurations
apiVersion: v1
kind: ConfigMap
metadata:
  name: placeholder-staging-overlay
  annotations:
    docs.ocp-virt.io/category: "kustomize"
    docs.ocp-virt.io/complexity: "intermediate"
    docs.ocp-virt.io/environment: "staging"
    docs.ocp-virt.io/description: "Placeholder for staging environment Kustomize overlay"
    docs.ocp-virt.io/prerequisites: "kustomize,staging-cluster"
    docs.ocp-virt.io/use-cases: "staging,pre-production,validation"
data:
  readme: |
    This directory will contain staging overlay configurations including:
    - Staging-specific kustomization.yaml
    - Pre-production environment settings
    - Validation and testing configurations
    - Performance testing components 