# This file ensures the gcp directory is committed to Git
# Remove this file when adding actual GCP-specific overlay configurations
apiVersion: v1
kind: ConfigMap
metadata:
  name: placeholder-gcp-overlay
  annotations:
    docs.ocp-virt.io/category: "kustomize"
    docs.ocp-virt.io/complexity: "advanced"
    docs.ocp-virt.io/environment: "cloud"
    docs.ocp-virt.io/description: "Placeholder for GCP-specific Kustomize overlay"
    docs.ocp-virt.io/prerequisites: "kustomize,gcp-openshift,gce-pd-csi"
    docs.ocp-virt.io/use-cases: "gcp-cloud,persistent-disk,vpc-networking"
data:
  readme: |
    This directory will contain GCP-specific overlay configurations including:
    - GCE Persistent Disk storage configurations
    - VPC and firewall rule optimizations
    - GCP load balancer configurations
    - Compute Engine machine type optimizations 