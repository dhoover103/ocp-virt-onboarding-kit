# This file ensures the backup directory is committed to Git
# Remove this file when adding actual backup component configurations
apiVersion: v1
kind: ConfigMap
metadata:
  name: placeholder-backup-components
  annotations:
    docs.ocp-virt.io/category: "kustomize"
    docs.ocp-virt.io/complexity: "advanced"
    docs.ocp-virt.io/environment: "cloud,bare-metal,hybrid"
    docs.ocp-virt.io/description: "Placeholder for backup Kustomize components"
    docs.ocp-virt.io/prerequisites: "kustomize,backup-tools"
    docs.ocp-virt.io/use-cases: "backup,disaster-recovery,data-protection"
data:
  readme: |
    This directory will contain backup Kustomize components including:
    - Velero backup configurations
    - VM snapshot components
    - Storage backup components
    - Backup scheduling configurations 