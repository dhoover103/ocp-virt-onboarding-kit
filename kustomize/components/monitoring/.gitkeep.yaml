# This file ensures the monitoring directory is committed to Git
# Remove this file when adding actual monitoring component configurations
apiVersion: v1
kind: ConfigMap
metadata:
  name: placeholder-monitoring-components
  annotations:
    docs.ocp-virt.io/category: "kustomize"
    docs.ocp-virt.io/complexity: "intermediate"
    docs.ocp-virt.io/environment: "cloud,bare-metal,hybrid"
    docs.ocp-virt.io/description: "Placeholder for monitoring Kustomize components"
    docs.ocp-virt.io/prerequisites: "kustomize,prometheus,grafana"
    docs.ocp-virt.io/use-cases: "monitoring,observability,alerting"
data:
  readme: |
    This directory will contain monitoring Kustomize components including:
    - Prometheus monitoring configurations
    - Grafana dashboard components
    - AlertManager rule components
    - VM-specific monitoring configurations 