# This file ensures the csi directory is committed to Git
# Remove this file when adding actual CSI storage configurations
apiVersion: v1
kind: ConfigMap
metadata:
  name: placeholder-csi
  annotations:
    docs.ocp-virt.io/category: "storage"
    docs.ocp-virt.io/complexity: "intermediate"
    docs.ocp-virt.io/environment: "cloud,bare-metal,hybrid"
    docs.ocp-virt.io/description: "Placeholder for Container Storage Interface configurations"
    docs.ocp-virt.io/prerequisites: "csi-drivers"
    docs.ocp-virt.io/use-cases: "cloud-storage,block-storage,file-storage"
data:
  readme: |
    This directory will contain CSI storage configurations including:
    - StorageClass definitions for various CSI drivers
    - CSI driver deployment manifests
    - Volume snapshot configurations
    - Cloud provider specific storage classes 