# This file ensures the schemas directory is committed to Git
# Remove this file when adding actual metadata schema configurations
apiVersion: v1
kind: ConfigMap
metadata:
  name: placeholder-schemas
  annotations:
    docs.ocp-virt.io/category: "metadata"
    docs.ocp-virt.io/complexity: "advanced"
    docs.ocp-virt.io/environment: "cloud,bare-metal,hybrid"
    docs.ocp-virt.io/description: "Placeholder for metadata schema configurations"
    docs.ocp-virt.io/prerequisites: "json-schema,yaml-schema"
    docs.ocp-virt.io/use-cases: "schema-validation,data-modeling,api-documentation"
data:
  readme: |
    This directory will contain metadata schema configurations including:
    - JSON Schema definitions
    - OpenAPI specifications
    - Kubernetes CRD schemas
    - Validation rule definitions 