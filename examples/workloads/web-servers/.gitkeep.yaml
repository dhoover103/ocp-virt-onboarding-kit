# This file ensures the web-servers directory is committed to Git
# Remove this file when adding actual web server workload configurations
apiVersion: v1
kind: ConfigMap
metadata:
  name: placeholder-web-servers
  annotations:
    docs.ocp-virt.io/category: "workloads"
    docs.ocp-virt.io/complexity: "intermediate"
    docs.ocp-virt.io/environment: "cloud,bare-metal,hybrid"
    docs.ocp-virt.io/description: "Placeholder for web server workload configurations"
    docs.ocp-virt.io/prerequisites: "openshift-virtualization,networking"
    docs.ocp-virt.io/use-cases: "apache,nginx,iis,load-balancing"
data:
  readme: |
    This directory will contain web server workload configurations including:
    - Apache HTTP server VMs
    - Nginx server configurations
    - IIS server VMs
    - Load balancer configurations 