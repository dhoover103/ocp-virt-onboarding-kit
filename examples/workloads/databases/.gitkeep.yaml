# This file ensures the databases directory is committed to Git
# Remove this file when adding actual database workload configurations
apiVersion: v1
kind: ConfigMap
metadata:
  name: placeholder-databases
  annotations:
    docs.ocp-virt.io/category: "workloads"
    docs.ocp-virt.io/complexity: "advanced"
    docs.ocp-virt.io/environment: "cloud,bare-metal,hybrid"
    docs.ocp-virt.io/description: "Placeholder for database workload configurations"
    docs.ocp-virt.io/prerequisites: "openshift-virtualization,persistent-storage"
    docs.ocp-virt.io/use-cases: "database-vms,mysql,postgresql,oracle"
data:
  readme: |
    This directory will contain database workload configurations including:
    - MySQL VM configurations
    - PostgreSQL VM configurations
    - Oracle database VMs
    - Database clustering configurations 