# This file ensures the network-config directory is committed to Git
# Remove this file when adding actual Ansible role configurations
apiVersion: v1
kind: ConfigMap
metadata:
  name: placeholder-network-config-role
  annotations:
    docs.ocp-virt.io/category: "automation"
    docs.ocp-virt.io/complexity: "intermediate"
    docs.ocp-virt.io/environment: "cloud,bare-metal,hybrid"
    docs.ocp-virt.io/description: "Placeholder for network configuration Ansible role"
    docs.ocp-virt.io/prerequisites: "ansible,nmstate-operator"
    docs.ocp-virt.io/use-cases: "network-automation,bridge-config,interface-config"
data:
  readme: |
    This directory will contain Ansible role for network configuration including:
    - tasks/ - Ansible tasks for network setup
    - vars/ - Variable definitions
    - templates/ - Jinja2 templates for network configs
    - defaults/ - Default variable values 