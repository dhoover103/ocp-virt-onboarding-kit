# This file ensures the setup directory is committed to Git
# Remove this file when adding actual setup script configurations
apiVersion: v1
kind: ConfigMap
metadata:
  name: placeholder-setup-scripts
  annotations:
    docs.ocp-virt.io/category: "scripts"
    docs.ocp-virt.io/complexity: "intermediate"
    docs.ocp-virt.io/environment: "cloud,bare-metal,hybrid"
    docs.ocp-virt.io/description: "Placeholder for setup script configurations"
    docs.ocp-virt.io/prerequisites: "bash,openshift-cli"
    docs.ocp-virt.io/use-cases: "cluster-setup,environment-preparation,automation"
data:
  readme: |
    This directory will contain setup scripts including:
    - Cluster preparation scripts
    - Operator installation scripts
    - Environment configuration scripts
    - Prerequisites validation scripts 