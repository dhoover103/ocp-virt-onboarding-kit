# This file ensures the validation directory is committed to Git
# Remove this file when adding actual validation script configurations
apiVersion: v1
kind: ConfigMap
metadata:
  name: placeholder-validation-scripts
  annotations:
    docs.ocp-virt.io/category: "scripts"
    docs.ocp-virt.io/complexity: "intermediate"
    docs.ocp-virt.io/environment: "cloud,bare-metal,hybrid"
    docs.ocp-virt.io/description: "Placeholder for validation script configurations"
    docs.ocp-virt.io/prerequisites: "bash,yaml-tools,validation-tools"
    docs.ocp-virt.io/use-cases: "yaml-validation,configuration-validation,compliance-checking"
data:
  readme: |
    This directory will contain validation scripts including:
    - YAML syntax validation
    - Kubernetes resource validation
    - Security policy validation
    - Best practices compliance checks 