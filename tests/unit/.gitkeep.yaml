# This file ensures the unit directory is committed to Git
# Remove this file when adding actual unit test configurations
apiVersion: v1
kind: ConfigMap
metadata:
  name: placeholder-unit-tests
  annotations:
    docs.ocp-virt.io/category: "testing"
    docs.ocp-virt.io/complexity: "basic"
    docs.ocp-virt.io/environment: "cloud,bare-metal,hybrid"
    docs.ocp-virt.io/description: "Placeholder for unit test configurations"
    docs.ocp-virt.io/prerequisites: "test-framework"
    docs.ocp-virt.io/use-cases: "unit-testing,yaml-validation,syntax-checking"
data:
  readme: |
    This directory will contain unit test configurations including:
    - YAML syntax validation tests
    - Kubernetes resource validation
    - Template rendering tests
    - Configuration validation scripts 